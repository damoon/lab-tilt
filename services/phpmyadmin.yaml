apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin
spec:
  ports:
    - name: pma
      port: 80
  selector:
    app: phpmyadmin
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: phpmyadmin
spec:
  selector:
    matchLabels:
        app: phpmyadmin
  strategy:
    type: Recreate
  replicas: 1
  template:
    metadata:
      labels:
        app: phpmyadmin
    spec:
      containers:
        - name: phpmyadmin
          image: phpmyadmin/phpmyadmin:latest@sha256:58f193cce63efec851a939bd8226fecdfd72a8a9ba7d8310cf6ac6d613059aca
          env:
            - name: PMA_USER
              value: username
            - name: PMA_PASSWORD
              value: password
            - name: PMA_HOSTS
              value: mariadb
          ports:
            - name: pma
              containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
