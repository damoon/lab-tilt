apiVersion: v1
kind: Service
metadata:
  name: opensearch
spec:
  ports:
    - name: api
      port: 9200
    - name: cluster
      port: 9600
  selector:
    app: opensearch
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: opensearch
spec:
  selector:
    matchLabels:
        app: opensearch
  strategy:
    type: Recreate
  replicas: 1
  template:
    metadata:
      labels:
        app: opensearch
    spec:
      containers:
        - name: opensearch
          image: opensearchproject/opensearch:2.19.6@sha256:e321cb03c643874c42240458a618db56bc1e4fc143b10c41bec653542bc6851b
          env:
            - name: discovery.type
              value: single-node
          ports:
            - name: api
              containerPort: 9200
            - name: cluster
              containerPort: 9600
          readinessProbe:
            httpGet:
              path: /
              scheme: HTTPS
              port: 9200
              httpHeaders:
                - name: Authorization
                  value: Basic YWRtaW46YWRtaW4=
          volumeMounts:
            - name: data
              mountPath: /usr/share/opensearch/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: opensearch
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opensearch
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
